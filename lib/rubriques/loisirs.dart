import 'package:flutter/material.dart';

class Loisirs extends StatelessWidget {
  const Loisirs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context){
    return const Scaffold(
      body: Text("Mon Expériences"),
    );
  }
}